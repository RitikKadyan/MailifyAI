{
  "name": "marketing-email-generator",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@testing-library/react": "^16.1.0",
    "axios": "^1.7.9",
    "cra-template-typescript": "1.2.0",
    "formik": "^2.4.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^4.2.4",
    "yup": "^1.5.0"
  },
  "scripts": {
    "clean": "rm -rf build",
    "predeploy": "npm run clean && npm run build",
    "deploy": "gh-pages -d build --repo https://x-access-token:${GITHUB_TOKEN}@github.com/RitikKadyan/marketing-email-generator.git",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "homepage": "https://RitikKadyan.github.io/marketing-email-generator",
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "rimraf": "^6.0.1"
  }
}
